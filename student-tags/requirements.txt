# Install everything with:   pip install -r requirements.txt
pandas>=1.5
openpyxl>=3.1
reportlab>=4.0
Pillow>=9.0
arabic-reshaper>=3.0
python-bidi>=0.4.2

# only needed for export_card_images.py (one PNG per student)
pymupdf>=1.23
